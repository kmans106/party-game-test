# Party Game

Phase 1 scaffold for a browser-based multiplayer drawing and guessing game.

## Workspace

- `packages/shared`: shared types, constants, and event names.
- `packages/server`: Express + Socket.IO backend bootstrap.
- `packages/client`: React + Vite frontend bootstrap.

## Scripts

- `npm install`
- `npm run dev`
- `npm run typecheck`
