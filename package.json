{
  "name": "party-game",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace @party-game/server\" \"npm run dev --workspace @party-game/client\"",
    "test": "npm run test --workspace @party-game/server",
    "typecheck": "npm run typecheck --workspace @party-game/shared && npm run typecheck --workspace @party-game/server && npm run typecheck --workspace @party-game/client",
    "build": "npm run build --workspace @party-game/shared && npm run build --workspace @party-game/server && npm run build --workspace @party-game/client"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "typescript": "^5.8.2"
  }
}
